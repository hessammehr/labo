services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - labo-data:/app/data
    environment:
      - LABO_DATA_DIR=/app/data
      - LABO_JWT_SECRET=${LABO_JWT_SECRET:-change-me-in-production}

volumes:
  labo-data:
